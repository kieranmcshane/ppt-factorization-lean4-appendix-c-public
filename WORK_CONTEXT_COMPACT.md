# WORK_CONTEXT_COMPACT - Lean Agent Upper replacement

Last compacted: 2026-06-02.

Read this file first in a new Codex chat.

## Purpose

This is the replacement working memory for the old crashing thread:

```text
Agent 2.1 : upper
019e34b3-8f8a-7413-9eb6-a51801aaf2a7
```

Do not open that thread. Continue from local files.

## Minimal Files To Read First

1. `START_CODEX_SAFE.md`
2. `FORMALIZATION_BOARD_COMPACT.md`
3. `LEAN_FILE_ATLAS_COMPACT.md`
4. `CODEX_RECOVERY_CONTEXT.md`

Read the full `FORMALIZATION_BOARD.md` or `LEAN_FILE_ATLAS.md` only when a
specific theorem name is needed. They are long by design.

## Live Frontier

Upper:

- active file: `PptFactorization/AppendixBPipelineGraduate.lean`
- active gap: finite profile-count / Aubrun graduate relation counting.

Lower:

- active file:
  `PptFactorization/AristotleTargets/LowerMeanLimitConcreteChoices.lean`
- active endpoint:
  `...CatalanErrorMeanAndExponentialTailStack_k3_withMixedErrorComponents...`
- active leaves:
  Catalan-error/Wick at length three, exponential tail, mixed envelope, mixed
  smallness.

## Git State

Branch:

```text
master
```

The tree is not clean. There are tracked changes in Lean and TeX files and
many untracked formalization candidates. Do not discard anything.

A status/diff snapshot was saved in:

```text
/Users/kieranmcshane/.codex/diagnostics/project-restart-20260602
```

## Operating Rule

One chat, one leaf. Do not let a new chat absorb the whole project history.
When a leaf changes, update `FORMALIZATION_BOARD_COMPACT.md` before ending.

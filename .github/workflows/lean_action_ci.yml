name: Lean CI

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5
      - name: Build with pinned Lean/Lake project
        uses: leanprover/lean-action@v1
        with:
          build: true

# LEAN_FILE_ATLAS_COMPACT

Last compacted: 2026-06-02.

This is the short navigation atlas. Use the full `LEAN_FILE_ATLAS.md` only for
specific theorem lookup.

## Atlas Before

The full atlas and board had many valid entries but were too large as startup
context. A new Codex chat could waste context re-reading old wrappers,
diagnostic endpoints, and repeated audit summaries.

## Atlas After

### Upper Route

Frontier:

```text
gammaExpectation_pipeline_to_spherical_bound_of_profile_count_sum_canonical_Qone
```

Matched declaration:

```text
AppendixB.gammaExpectation_pipeline_to_spherical_bound_of_profile_count_sum_canonical_Qone
```

File:

```text
PptFactorization/AppendixBPipelineGraduate.lean
```

Classification:

```text
active endpoint
```

Plain math:

The upper expectation/spherical bridge is reduced to a finite profile-count
bound with Aubrun's explicit polynomial `Q(k)=k(2k)^36`.

Next action:

Prove or import the finite `wickRelationProfileCount` profile-count sum bound.

### Lower Route

Frontier:

```text
lower_eventual_log_over_spikeSpeed_concreteModel_of_paperFacingPTCatalanErrorMeanAndExponentialTailStack_k3_withMixedErrorComponents_splitMixedWordBudget
```

Matched declaration:

```text
AppendixB.lower_eventual_log_over_spikeSpeed_concreteModel_of_paperFacingPTCatalanErrorMeanAndExponentialTailStack_k3_withMixedErrorComponents_splitMixedWordBudget
```

File:

```text
PptFactorization/AristotleTargets/LowerMeanLimitConcreteChoices.lean
```

Classification:

```text
active endpoint
```

Plain math:

The length-three lower bound is reduced to Catalan-error/Wick, exponential
tail, and honest mixed-error components for the same `errMix`.

Next action:

Pick one remaining leaf:

- Catalan-error/Wick at length three;
- exponential deviation tail at length three;
- mixed local-expansion envelope;
- mixed eventual smallness.

### Diagnostic / Not Live

The fixed-`M` PT scale-comparison route is diagnostic only at length three.
Do not treat it as the live unconditional lower route.

Relevant no-go:

```text
AppendixB.lower_paperFacingPTScaleComparisonPacket_three_not_uniform
```

## Next Adapter

For each future chat, choose exactly one adapter or leaf and record:

- theorem name,
- file and line,
- build command,
- axiom/sorry audit result,
- whether it changes the live frontier.
